const LinkedListRunner = require('./lists/linked-list-runner');
const StackListRunner = require('./lists/stack-list-runner');
const QueueListRunner = require('./lists/queue-list-runner');

QueueListRunner.run();
